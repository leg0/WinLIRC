macro(add_tool tool_name)
  option(EnableTool_${tool_name} "Enable tool ${tool_name}" ON)
  add_subdirectory(${tool_name})
endmacro()

add_tool(Graph)
add_tool(RawCodes)
add_tool(RawMessages)
add_tool(Transmit)
add_tool(XBMC_Keymap)
